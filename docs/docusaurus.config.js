const { themes } = require('prism-react-renderer');
const lightCodeTheme = themes.github;
const darkCodeTheme = themes.dracula;

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: 'FoundryVTT REST API Relay',
  tagline: 'Message relay server for the foundryvtt-rest-api Foundry Module',
  favicon: 'img/favicon.svg',

  // Set the production url of your site here
  url: 'https://foundryrestapi.com',
  // Set the /<baseUrl>/ pathname under which your site is served
  baseUrl: '/docs/',

  // Add trailing slash configuration
  trailingSlash: false,

  onBrokenLinks: 'warn',
  onBrokenMarkdownLinks: 'warn',

  // Even if you don't use internationalization, you can use this field to set
  // useful metadata like html lang. For example, if your site is Chinese, you
  // may want to replace "en" with "zh-Hans".
  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          path: './md',
          routeBasePath: '/',
          sidebarPath: './sidebars.js',
        },
        blog: false, // Disable blog
        theme: {
          customCss: './src/css/custom.css',
        },
      }),
    ],
  ],

  plugins: [
    // API documentation is now generated by running: pnpm run docs:update
    // This runs before the build and includes both source docs and test examples
    // No need to regenerate during build as it would overwrite test examples
  ],

  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      // Replace with your project's social card
      image: 'img/docusaurus-social-card.jpg',
      navbar: {
        title: 'FoundryVTT REST API Relay',
        logo: {
          alt: 'FoundryVTT Logo',
          src: 'img/logo.svg',
        },
        items: [
          {
            type: 'docSidebar',
            sidebarId: 'tutorialSidebar',
            position: 'left',
            label: 'Documentation',
          },
          {
            to: '/api/',
            label: 'API Reference',
            position: 'left',
          },
          {
            href: 'https://github.com/ThreeHats/foundryvtt-rest-api-relay',
            label: 'GitHub',
            position: 'right',
          },
          {
            href: '/',
            label: '← Dashboard',
            position: 'right',
          },
        ],
      },
      footer: {
        style: 'dark',
        links: [
          {
            title: 'Docs',
            items: [
              {
                label: 'Getting Started',
                to: '/intro',
              },
              {
                label: 'API Reference',
                to: '/api/',
              },
            ],
          },
          {
            title: 'Community',
            items: [
              {
                label: 'Discord',
                href: 'https://discord.gg/U634xNGRAC',
              },
            ],
          },
          {
            title: 'More',
            items: [
              {
                label: 'GitHub',
                href: 'https://github.com/ThreeHats/foundryvtt-rest-api-relay',
              },
            ],
          },
        ],
      },
      prism: {
        theme: lightCodeTheme,
        darkTheme: darkCodeTheme,
      },
      colorMode: {
        defaultMode: 'dark',
        disableSwitch: false,
        respectPrefersColorScheme: false,
      },
      ...(process.env.ALGOLIA_APP_ID && process.env.ALGOLIA_SEARCH_API_KEY
        ? {
            algolia: {
              appId: process.env.ALGOLIA_APP_ID,
              apiKey: process.env.ALGOLIA_SEARCH_API_KEY,
              indexName: process.env.ALGOLIA_INDEX_NAME || 'foundryvtt-rest-api-relay',
              contextualSearch: false,
              searchPagePath: 'search',
            },
          }
        : {}),
    }),
};

export default config;
